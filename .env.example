# Root .env.example — copy to .env for local development.
# Each app also has its own .env.example in its directory.
# This root file is for docker-compose.yml which reads from here.

# Optional: set to use OpenAI instead of local Ollama
OPENAI_API_KEY=
